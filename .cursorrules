# Cursor 编程规则

## 文档管理原则

### 重要文档
- **根目录下的README.md和CHANGELOG.md是最重要的文档，必须保留**
- **每次更新必须是在用户对代码修改完全确认之后进行**
- 不允许随意修改这两个文档，除非用户明确要求
- changelog要确保使用正确的日期
- **每次当我确认readme和changelog可以修改时，同时根据改动内容适当修改api_reference, database_schema, implementation_status**
- **changelog里面不需要很详细的改动实现，但要说明改动的功能、api、组件、数据库变化等等**

### 文档精简原则
- 避免重复文档，相同内容应该合并或删除
- 优先保留用户明确要求保留的文档
- 删除过时或已合并到其他文档的内容

### 文档组织结构
- **核心文档**：`api_reference.md`、`database_schema.md`、`implementation_status.md` 是必须保持更新的关键文档
- **分析文档**：每次有新功能先做讨论分析，完成分析后删除，关键结论添加到核心文档中
- **规划文档**：规划文档，完成后重命名为 `YYYYMMDD-filename.md` 格式
- **实现文档**：实现文档，完成后重命名为 `YYYYMMDD-filename.md` 格式，注意和规划文档的对应关系
- **完成标记**：当用户要求更新README和CHANGELOG时，同时将对应的实现文档重命名为带日期的格式，标记完成
- **文档清理**：定期清理过时文档，保持文档文件夹的整洁和可读性

## 编程规范

### 语言要求
- **始终使用中文回复用户**
- 以下基本命令你可以自己执行，无需确认：curl
- 代码注释可以使用中文或英文，但保持一致性

### 代码修改原则
- 在修改代码前，先分析现有代码结构
- 保持代码风格的一致性
- 修改后要确保功能正常
- **API 与测试**：凡涉及后端 **HTTP 接口或路由层** 的修改，须遵守下节「**API 与自动化测试（强制）**」；须保持 **API 兼容性**（破坏性变更须与用户约定后再做）。
- 如果测试用例失败，不要自行修改case的预期结果，要和用户确认
- client代码缺省不增加自动化测试用例，除非用户要求

### 调试和清理原则
- **cursor每次自我调试，最终需要将打开的服务端、客户端端口占用关闭

### 文档更新原则
- 重要文档的更新需要用户确认
- 保持文档的时效性和准确性
- 避免文档冗余

## 项目特定规则

### API设计
- 保持API接口的稳定性
- 遵循RESTful设计原则

### API 与自动化测试（强制）
- 凡修改 `backend/app/api/` 路由或对外 HTTP 契约，必须同步补充/更新 `backend/tests/` 中对应用例
- 后端测试使用 pytest + httpx AsyncClient；本地可无真实 PostgreSQL（SQLite 内存库）
- 运行：`cd backend && pip install -r requirements-dev.txt && pytest`，或根目录 `npm run test:backend`
- 测试失败时不得擅自改断言预期，须与用户确认
- client（前端）缺省不增加自动化测试，除非用户要求

## 沟通原则

- 在不确定用户意图时，主动询问
- 提供清晰的解释和建议
- 尊重用户的编程偏好和项目结构
